{
  "routes": [
    {
      "src": "/api/(.*)",
      "methods": ["GET", "POST", "PUT", "DELETE"],
      "dest": "/api/$1",
      "headers": {
        "x-vercel-id": "api"
      },
      "config": {
        "api": {
          "timeout": 50000
        }
      }
    }
  ]
}
